spring:
  datasource:
    url: jdbc:postgresql://dpg-cngg4121hbls738heeg0-a:5432/macaela
    username: ${PROD_DB_USERNAME}
    password: ${PROD_DB_PASSWORD}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE
server:
  error:
    include-stacktrace: never
api:
  security:
    secret: ${JWT_SECRET}